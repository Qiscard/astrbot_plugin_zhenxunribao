# Changelog

本文件记录真寻日报插件的 notable changes。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.7.0] - 2026-09-20

### 新增

- **主题方案（方案A）**：新增 8 套预设主题——真寻粉（默认）/ 樱花粉 / 薰衣草紫 / 晴空蓝 / 薄荷绿 / 暖阳橙 / 抹茶绿 / 夜幕蓝。参考 `astrbot_plugin_bot_menu` 的预设主题组织方式：每个主题由背景、面板、描边、标题、文字等 11 个颜色变量组成，选中即整图换肤。编辑器新增「🎨 主题」工具栏按钮与画布底部主题条，预设卡片带色板预览；编辑画布通过 `--t-*` CSS 变量实时联动，真实渲染通过注入 `:root` 变量覆盖块实现。
- **每日英语显示数量**：顶部来源为英语时新增「单词数量」设置（`top_english_count`，1-5），一次可展示多个单词，每个单词含音标、释义与例句；编辑弹窗、画布预览与真实渲染同步支持。

### 变更

- **英语模块条数上限对齐**：中间模块「每日英语」条数上限由 30 收窄为 5（`count_max`），与接口单次实际返回能力一致，避免设置无效条数。
- **编辑器主题元数据**：`/meta` 接口新增 `theme_presets` 字段，编辑器预设卡片以后端为唯一数据源（本地仅保留离线兜底）。

## [1.6.0] - 2026-09-19

### 新增

- **中间模块统一列表**：中部模块1/中部模块2 与附加模块整合为统一的「中间模块」，可自由添加、排序、隐藏，同类型模块可重复添加；添加/编辑弹窗按类型分类（番剧游戏 / 新闻资讯 / 文字内容）分组展示。
- **小黑盒游戏独立模块类型**：此前仅能作为中部模块1来源，现可作为普通中间模块自由添加。
- **汇率目标货币逐项编辑**：目标货币列表支持逐项添加、上移/下移、删除；每个目标货币独立与源货币换算、互不干扰。

### 变更

- **摸鱼日历编辑栏字段顺序**：调整为「类型 → 前缀 → 自定义内容 → 后缀 → 自定义时间」，与渲染行「前缀-内容-后缀-N天」一致。
- **顶部模块位置固定**：顶部模块始终保持在摸鱼日历侧方；摸鱼日历无计时项时显示占位，不再挤占顶部模块位置。

### 迁移

- 旧 `mid1_source/mid1_title/mid1_count/mid1_params` 与 `mid2_*` 固定位配置在读取时自动转为中间模块列表前两项（标题/条数/接口参数保留；类型已存在于列表时不重复迁移）。

## [1.5.0] - 2026-09-19

### 新增

- **摸鱼日历自定义文案**：每条计时项支持自定义前段文字（默认「距离」）与后段文字（默认「还剩」），如「离 国庆节 还有 N 天」。
- **周末计时项**：原「周期」类型更名为「周末」，仅可选择周六/周日，名称留空时自动显示「周六/周日」。
- **汇率多目标货币**：实时汇率支持一个源货币同时配置多个目标货币（`exchange_targets`，最多 6 个）。

### 变更

- **字体精简（约 -26MB）**：移除从未参与实际渲染的 HarmonyOS Sans Regular/Bold 与 Noto Sans SC Black；英文标题 MAHIRO NEWS 改用站酷方糖体（SSFangTangTi）。经 CDP 逐节点字体取证与像素级对比验证，渲染效果无变化，嵌入 HTML 体积减少约 55%（57MB → 25MB）。
- **摸鱼日历默认为空**：新配置不再预置节假日/周末计时项，由用户按需添加。
- **编辑器即时保存**：所有编辑操作（含固定模块位的接口参数选择）即时自动保存，无需点击「完成」。
- **模块命名明确化**：弹窗与画布标签明确区分「固定位」（摸鱼日历/顶部/中部模块1/中部模块2/底部引用）与「附加模块」（纵向排列）。

### 移除

- **自定义倒计时模块**：功能与摸鱼日历的自定义日期重复；旧 `modules` 中的 countdown 条目自动迁移为摸鱼日历自定义计时项。
- **每日人民日报接口**：接口仅返回 PDF 链接（24-54MB 电子版文件），无法在日报图片中直接查看；同时移除 `/人民日报` 聊天命令。

## [1.4.0] - 2026-09-19

### 新增

- **日报编辑器 Page（`pages/daily-editor/`）**：在 WebUI 中可视化编辑日报布局，支持画布点击编辑、真实渲染预览、模块增删与排序。
- **固定模块位与候选来源**：日报采用「摸鱼日历 + 顶部模块 + 中部模块1 + 中部模块2 + 底部引用」的固定布局，每个模块位从候选来源中单选。
- **固定模块位支持接口参数**：中部模块1（今日追番）可配置 `date`；中部模块2（AI早报）可配置 `type`/`date`，人民日报可配置 `date`。参数保存于 `mid1_params` / `mid2_params`，并真实传入接口。
- **接入新数据源**：Milora（AI早报、人民日报 PDF）、慕名API（追番、诗词、语录、答题、灯谜）、糖豆子（汇率、游戏、一言、谜语）、aa1.cn（短句）、每日英语等。
- **新增聊天指令**：`/歇后语`、`/B站封面`、`/缩写`、`/AI早报`、`/人民日报`、`/追番`、`/诗词`、`/语录`、`/答题`、`/灯谜`。
- **配置接口参数化**：编辑器「配置接口」区域按模块 Schema 渲染表单；插件配置界面只保留通用配置与 key/token。

### 变更

- **配置边界明确划分**：`_conf_schema.json` 只保存 API Key/Token、渲染、定时推送与 AI 问候语；日报布局与模块参数统一由数据目录的 `modules.json` 维护，每次生成日报时重新读取，改完免重载生效。
- **底部引用恢复多选**：`quote_sources` 支持多选，每次随机取其一；修复此前编辑器打开保存会把多选压成单选的问题。
- **模块元数据由后端驱动**：编辑器优先使用 `meta.module_types` 构建可添加模块列表，后端新增模块后 UI 自动同步。
- **`alapi_token` / `milora_api_key` 标记为 `secret`**：管理面板默认遮罩显示。
- **移除已废弃的 `@register` 装饰器**：AstrBot 会自动识别 `Star` 子类。
- **定时推送统一改走 `context.send_message`**：移除直接调用 OneBot `call_action("send_group_msg")` 的私有路径，推送现支持所有平台适配器；推送目标支持完整 `unified_msg_origin` 或已学习映射的纯群号。
- **重写定时调度器**：改为 30 秒间隔检查循环，配置变更约 30 秒内生效；以「最后推送日期」标记防止当日重复推送。
- **AI 问候语兜底修复**：替换为实际存在的 `provider_manager.get_insts()` 接口。
- **临时文件名改用 `uuid4`**，避免并发渲染时文件名冲突。

### 修复

- **修复顶部模块重复渲染**：顶部来源选择「每日英语」时，不再同时出现在顶部面板和自由模块区。
- **修复配置健壮性**：非法的 `modules`、`moyu_items`、倒计时 `items` 不再导致生成失败；嵌套条目做类型过滤；模块数量统一限制上限。
- **移除无效条数配置**：诗词、语录等单条接口不再显示无意义的「条数」设置。
- **修复顶部面板样式**：顶部面板按实际类型使用对应 CSS 类与图标，不再一律使用历史面板样式。
- **修复自动保存失败静默**：编辑器自动保存失败时会给出错误提示。
- **保留旧配置兼容**：旧版 `modules.json` 与旧版 `modules` 列表可继续读取，历史/引用的旧条目在读取时自动迁移，且不会在插件启动时静默重写用户文件。

### 文档

- README 与代码全面对齐：修正 Milora/ALAPI 的职责说明、DPR 范围为 1-6（默认 5）、补充固定模块位参数说明，并说明日报编辑器配置与聊天命令参数相互独立。

## [1.3.1] - 2026-09-06

### 安全

- **移除硬编码的 ALAPI Token**：原 Token 曾硬编码在 `holiday_api.py`、`zaobao_api.py`、`history_api.py`、`hitokoto_api.py` 四个文件中，现全部改为从插件配置读取（默认留空，仅使用免费数据源）。曾在日志中以 INFO 级别打印含 Token 的完整 URL 的问题一并修复。
- 如您使用过受影响版本，建议前往 [ALAPI 后台](https://admin.alapi.cn/user/login) 重置 Token。

### 变更

- **移除废弃的 `@register` 装饰器**：AstrBot 自 v3.5.19 起自动识别 `Star` 子类，该装饰器已废弃。
- **定时推送统一改走 `context.send_message`**：移除直接调用 OneBot `call_action("send_group_msg")` 的私有路径，推送现支持所有平台适配器（aiocqhttp、QQ 官方 API、Telegram 等）。推送目标支持完整 `unified_msg_origin` 或已学习映射的纯群号。
- **重写定时调度器**：改为 30 秒间隔检查循环，配置变更约 30 秒内生效（原先最坏滞后 24 小时）；修复插件重载时延迟启动任务未被跟踪、可能在卸载后继续推送的问题；以"最后推送日期"标记防止当日重复推送。
- **API 失败时不再展示编造数据**：移除假新闻、假新番、假历史事件、假节假日等兜底数据，失败板块显示"暂未获取到数据"占位提示。
- **AI 问候语兜底修复**：替换为实际存在的 `provider_manager.get_insts()` 接口（原 `get_all_providers()` 不存在，导致兜底逻辑静默失效）。
- **按引用模式按需请求**：`quote_mode` 为 `duji` 时不再请求今日一言，反之亦然。
- **临时文件名改用 `uuid4`**，路径处理全面 `pathlib` 化。
- **降低日志噪音**：原始数据与响应体日志降为 debug 级别。

### 文档

- README 与代码全面对齐：移除未实现的"B站热点 / IT资讯"功能描述、统一仓库地址、配置表与 `_conf_schema.json` 逐项一致、补充 `api_token` 说明。
- 重写 `test_apis.py` 以匹配当前 API 模块与签名。

## [1.3.0] 及更早版本

- 从 [nonebot-plugin-zxreport](https://github.com/HibiKier/nonebot-plugin-zxreport) 移植至 AstrBot，包含今日新番、60s读懂世界、历史上的今天、摸鱼日历、今日一言/毒鸡汤与定时推送等功能。更早版本的变更未在本文件中记录。
